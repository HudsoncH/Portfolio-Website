/**
 * Next.js config file. Left mostly empty on purpose — Next.js works out of
 * the box on Vercel with zero configuration here.
 *
 * Things you might add later:
 *  - `images: { remotePatterns: [...] }` if you use next/image with photos
 *    hosted on an external domain (not needed for local files in /public).
 *  - `output: 'export'` ONLY if you wanted a fully static export instead of
 *    Vercel's Next.js runtime. Don't add this unless you know you need it —
 *    it disables server features and isn't necessary for deploying to Vercel.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

module.exports = nextConfig;
